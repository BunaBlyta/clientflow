@AGENTS.md

Claude Code doesn't read AGENTS.md natively — this import line makes it load the same file Codex CLI reads, so there's one source of truth instead of two files drifting apart. Do not add project instructions below this line; add them to AGENTS.md instead.
