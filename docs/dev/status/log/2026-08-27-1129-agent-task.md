# 2026-08-27 11:29 — Codex — align notification timestamps as quiet metadata

## Changed

- Updated `mobile/components/NotificationRow.tsx` so the title can use the full text-column width and wrap naturally to two lines.
- Moved the timestamp to a small right-aligned metadata line below the title and before the body, preserving the existing title/body hierarchy and row interactions.
- Kept the existing typography, muted color, accessibility text, archive action, and responsive row structure unchanged.

## Verification

- `npx tsc --noEmit` from `mobile/`: passed.
- `git diff --check`: passed.
- No test files or test setup were present for a focused mobile test.
