# Web lane — match packages section base to light mode

- Removed only the dark Packages section base background so it now behaves like light mode and remains transparent.
- Left the hero gradient, localized package bubbles, cards, and Request section unchanged.
- No commit, checks, or push performed; waiting for the user’s instruction.
