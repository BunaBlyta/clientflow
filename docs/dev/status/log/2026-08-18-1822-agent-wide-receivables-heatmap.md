# 2026-08-18 18:22 — Use wide receivables card space

Updated the receivables heatmap to provide 364 days (52 complete weeks) and let the columns fill the available card width. The cells now grow naturally with the grid instead of leaving a large empty area on wide screens, while the weekday labels stay aligned with the seven heatmap rows.

No checks or commit run, per user instruction.
