# Agent B — lighten request placeholders

- Changed the light-mode Request a package input and textarea placeholders to soft light gray.
- Left entered values and the selected package dropdown text unchanged.
- No checks or commit run, per the user’s instruction.
