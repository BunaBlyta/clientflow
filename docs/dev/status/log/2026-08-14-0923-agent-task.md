### 2026-08-14 09:23 — Agent B — simplify public header controls

Changed:
- Removed the “Start a project” button from the marketing navbar.
- Added a compact globe-only language trigger for the marketing navbar while keeping the language menu and current locale behavior intact.
- Left the dashboard Settings language selector unchanged.

Tried and abandoned (what didn't work, and why):
- No alternate approach was needed; the shared selector now has an explicit compact mode so the public header can be minimal without changing the dashboard control.

Left for next session:
- The public header uses only the globe icon for language selection and the existing theme toggle/staff login controls.

Assumptions made (flag if wrong):
- “World logo” means a globe icon that still opens the language menu when clicked.

Blockers:
- No browser click-through was available in this session; test, typecheck, lint, and webpack build passed.
