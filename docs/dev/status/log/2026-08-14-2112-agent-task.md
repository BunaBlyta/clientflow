# Agent B — add How it works spacing

- Moved the How it works heading and process flow down slightly with increased top padding.
- Increased the section’s bottom padding to create more separation before the following content.
- Left all backgrounds, gradients, and colors unchanged.
- No checks or commit run, per the user’s instruction.
