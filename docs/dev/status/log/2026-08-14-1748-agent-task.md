# Web lane — connect timeline from first icon

## Changed

- Replaced the single fixed connector with per-step segments.
- Each segment begins at an icon center and reaches the next icon, so the first item is connected correctly.
- Existing light-mode shine and dark-mode connector styling remain in place.

## Verification

- Not run, per Buna’s instruction to hold checks and commits.
