# Web lane — simplify How it works and fix timeline icons

## Changed

- Replaced the How it works gradient and blur layers with a flat `#E8F8FE` surface matching Request a package.
- Explicitly placed the connector behind the step icons and cards.
- Made light-mode step icons solid glossy cyan with darker cyan glyphs so the line cannot show through them.
- Dark mode remains unchanged.

## Verification

- Not run, per Buna’s instruction to hold checks and commits.
