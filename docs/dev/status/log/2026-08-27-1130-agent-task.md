# 2026-08-27 11:30 — Codex — inline notification timestamps

## Changed

- Updated `mobile/components/NotificationRow.tsx` so each timestamp follows its notification title inline with a small `·` separator.
- Kept the timestamp as a nested muted metadata `Text`, allowing the title and timestamp to wrap together naturally within the existing text column.
- Removed the separate metadata line treatment without changing the notification body hierarchy, archive action, press behavior, or accessibility content.

## Verification

- `npx tsc --noEmit` from `mobile/`: passed.
- `git diff --check`: passed.

## Handoff

- No i18n keys or API files were changed.
