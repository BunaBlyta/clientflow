### 2026-08-14 09:19 — Agent B — select theme-specific logo assets

Changed:
- Added the shared `BrandLogo` component, which shows `/light-logo.png` in light mode and `/dark-logo.png` when the document has the dark theme class.
- Updated the marketing navbar and dashboard sidebar to use `BrandLogo` instead of the removed `/logo.png` path.
- Kept the supplied light and dark asset files as the source of truth and removed the obsolete single-logo reference.

Tried and abandoned (what didn't work, and why):
- The earlier single transparent-logo replacement did not meet the requested visual result. Once separate light/dark assets were supplied, the implementation was changed to select those exact files per theme rather than trying to process one image further.

Left for next session:
- No further logo consumer should reference `/logo.png`; use `BrandLogo` for new web surfaces.

Assumptions made (flag if wrong):
- CSS theme-class switching is preferred here because it avoids hydration mismatch and follows the existing `next-themes` `class` configuration.

Blockers:
- No browser click-through was available in this session; test, typecheck, lint, and webpack build passed.
