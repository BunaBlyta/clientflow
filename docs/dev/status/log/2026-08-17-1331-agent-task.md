### 2026-08-17 13:31 — Codex — add safe processing payment check

Changed:
- Added a localized “Check payment status” action to the mobile invoice detail screen while an invoice is processing.
- The action refreshes the invoice only; it does not create another Stripe Checkout Session while the first payment may still complete.
- Once the webhook reports `FAILED`, the existing retry-payment action appears automatically.

Tried and abandoned (what didn't work, and why):
- No alternative retry behavior was added for a still-processing payment because opening a second session could charge the client twice if the original payment later succeeds.

Left for next session:
- Confirm the processing-state check and failed-state retry with a fresh mobile click-through.

Assumptions made (flag if wrong):
- A processing payment must remain non-retryable until Stripe or the webhook confirms failure.

Blockers:
- No connected mobile simulator or device was available for the click-through.
