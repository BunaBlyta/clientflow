# Mobile tab label clipping and logout confirmation order

Date: 2026-08-13 15:04
Lane: Mobile (Agent C)

## Completed

- Removed the custom fixed tab-bar height and vertical padding so Expo Router
  can use its calculated tab-bar geometry without clipping the Projects,
  Notifications, or Account labels under a white strip.
- Moved “Confirm log out?” below the equal-width Cancel and Log out buttons.
- Kept the standalone Log out action full width and the confirmation actions
  50/50 across their row.

## Verification

- `npx tsc --noEmit` from `mobile/`: passed.
- Root `npm run test`: passed — 32 test files, 132 tests.
- Root `npm run typecheck`: passed.
- Root `npm run lint`: passed.
- `git diff --check`: passed.
- No device, simulator, or interactive Expo web testing was performed.
