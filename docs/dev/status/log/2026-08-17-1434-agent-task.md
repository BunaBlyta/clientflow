### 2026-08-17 14:34 — Codex — keep delayed Checkout payments pending

Changed:
- Prevented `checkout.session.completed` from marking delayed payments Paid when Stripe reports `payment_status: unpaid`.
- Delayed payments now remain pending until `checkout.session.async_payment_succeeded` arrives.
- Added a regression test for the unpaid completed-session state.

Tried and abandoned (what didn't work, and why):
- Treating every `checkout.session.completed` event as a successful payment was unsafe for delayed payment methods because completion can mean only that the customer submitted the payment.

Left for next session:
- Test an actual delayed payment after enabling the method in Stripe test mode.

Assumptions made (flag if wrong):
- A Checkout Session with `payment_status: unpaid` must not advance a Clientflow invoice to Paid.

Blockers:
- No delayed-payment Checkout click-through was available in this turn.
