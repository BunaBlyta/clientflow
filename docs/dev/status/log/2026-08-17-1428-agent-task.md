### 2026-08-17 14:28 — Codex — support delayed Checkout webhook events

Changed:
- Added delayed Checkout success handling through `checkout.session.async_payment_succeeded`.
- Added delayed Checkout failure handling through `checkout.session.async_payment_failed`.
- Added regression tests for both event types.

Tried and abandoned (what didn't work, and why):
- No automatic Stripe Dashboard configuration was attempted; the webhook subscription is external configuration and must be enabled on the exact deployed endpoint.

Left for next session:
- In Stripe Dashboard, add the two async event types and `checkout.session.expired` to the production webhook endpoint if delayed methods are enabled.

Assumptions made (flag if wrong):
- Delayed Checkout events carry the same Checkout Session metadata used to identify the invoice.

Blockers:
- No Stripe Dashboard configuration click-through was performed in this turn.
