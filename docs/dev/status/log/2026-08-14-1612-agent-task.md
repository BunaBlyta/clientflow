# Web lane — fix hero gradient visibility

## Changed

- Moved the hero gradient layer above the page background and below the hero content.
- Kept the gradient styling and content unchanged; this fixes the invisible light-mode render caused by its negative stacking level.

## Verification

- Not run, per Buna’s instruction to hold checks and commits.
