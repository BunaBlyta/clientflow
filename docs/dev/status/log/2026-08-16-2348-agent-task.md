### 2026-08-16 23:48 — Agent B — keep package editing inside Settings

Changed:
- Replaced the nested Edit package dialog with an inline edit form rendered inside the existing Settings modal.
- Kept save, delete confirmation, API calls, toasts, and package list updates unchanged.
- Added a cancel action that returns to the package row without closing Settings.

Tried and abandoned:
- None.

Left for next session:
- Create package still uses its own dialog because this request only covered the Edit action.

Assumptions made:
- The existing destructive delete confirmation can remain a separate modal because it is a confirmation step, not the edit form itself.

Blockers:
- No browser verification was run.
