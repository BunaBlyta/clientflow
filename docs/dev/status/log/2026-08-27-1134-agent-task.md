# 2026-08-27 11:34 — Codex — reset notes composer height after send

## Changed

- Added controlled `TextInput` height handling to `mobile/app/(app)/projects/[id]/notes.tsx` using native content size clamped between the existing compact and expanded bounds.
- Reset the input height to its compact minimum alongside the successful draft clear, while keeping the input mounted and focused so the keyboard is not dismissed.
- Kept long active drafts scrollable at the existing maximum height and guarded empty-content events so a stale native measurement cannot re-expand the cleared input.
- No API behavior or request payloads were changed.

## Verification

- `npx tsc --noEmit` from `mobile/`: passed.
- `git diff --check`: passed.

## Handoff

- No mobile test setup was added; no focused test runner exists in the Expo project.
