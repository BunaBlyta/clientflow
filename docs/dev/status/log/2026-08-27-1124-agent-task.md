# 2026-08-27 11:24 — Codex — translate dynamic invoice notification descriptions

## Changed

- Added a focused extractor for user-entered invoice descriptions in the known `INVOICE_ISSUED` / `Invoice issued`, `Invoice sent`, and `EXTRA_CHARGE_CREATED` / `Additional invoice sent` notification variants.
- Reused the deterministic localized invoice body template and asynchronously translated only the extracted description through the existing authenticated translation client.
- Updated notification rows and the in-app banner to replace the description when available while retaining the original description and localized wrapper on failure or while loading.
- No new i18n keys were needed, and fixed system notification copy is not sent for translation.

## Verification

- Mobile has no Vitest/Jest configuration or test files, so no focused mobile test was added.
- `npx tsc --noEmit` from `mobile/`: passed.
- `git diff --check`: passed.

## Handoff

- No API files were changed. The patch is limited to mobile notification formatting/rendering plus the mobile lane status/log.
