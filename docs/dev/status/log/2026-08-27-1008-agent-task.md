# 2026-08-27 10:08 — Codex — finish mobile notification, note, and translation behavior

## Changed

- Added tolerant notification response parsing for the documented `{ notifications, page, pageSize, hasMore }` envelope and the legacy array response.
- Added bounded page loading, archive/restore actions, Active/Archived views, realtime ID deduplication, and unread-state bookkeeping in the mobile data store.
- Kept archive independent from read state in the mobile client. The archive button never calls the read endpoint or locally changes `read`.
- Updated the note composer and data store for the API's 10,000-character limit. Long drafts remain scrollable and intact after a failed request, with localized validation and character-count feedback.
- Added non-blocking user-content translation for note bubbles, NEW_NOTE notifications, and the in-app banner. Requests are authenticated, shared by content/language, cached on success, cooled down after failures, and always fall back to the original content.
- Added the required English, Albanian, and German strings for archive actions and long-note validation without changing the existing visual direction.

## Verification

- `npx tsc --noEmit` from `mobile/`: passed.
- `git diff --check`: passed.
- No device or simulator was available for on-device verification.
- No package installation, Prisma command, or `app/api/**` edit was made.

## API handoff

- Buna must run `npx prisma migrate deploy` from the repository root after reviewing the API lane's notification archive migration.
- A concurrent API-lane `/api/translate` implementation is visible in the checkout but remains untracked and outside this mobile commit. Once committed/deployed with server-side `DEEPL_API_KEY`, the mobile client can use it; the client does not call DeepL or Groq directly, and the original note/message text remains visible whenever the route is unavailable.
- The current API page response has no global unread total. The mobile badge/count is exact for the notifications loaded or received in the client; an API unread-count field/endpoint is still needed for exact counts beyond the first page.
