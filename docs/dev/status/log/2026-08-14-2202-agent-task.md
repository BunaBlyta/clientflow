# Agent B — restore page scrollbar

- Added a visible slim page scrollbar with cyan thumb styling for light and dark modes.
- Added a stable scrollbar gutter to prevent layout shifting.
- Preserved `.scrollbar-none` behavior for components that intentionally hide their own scrollbars.
- No checks or commit run, per the user’s instruction.
