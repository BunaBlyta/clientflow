# Clickable mobile notification targets

Date: 2026-08-13 15:26
Lane: Mobile (Agent C)

## Completed

- Added optional `requestId` to the mobile `Notification` type alongside the
  API-provided `projectId` and `invoiceId` fields.
- Centralized notification target selection using IDs only:
  - project + invoice → `/projects/<projectId>/invoices/<invoiceId>`
  - project, including request-linked notifications → `/projects/<projectId>`
  - no usable project target → no navigation
- Preserved the existing mark-as-read flow, loading/error states, duplicate
  press protection, and Expo Router navigation.
- Kept notification refresh and mark-read responses as the full API objects so
  target IDs are not discarded.

## Verification

- `npx tsc --noEmit` from `mobile/`: passed.
- Root `npm run test`: passed — 32 test files, 132 tests.
- Root `npm run typecheck`: passed.
- Root `npm run lint`: passed.
- `git diff --check`: passed.
- No device, simulator, or interactive Expo web testing was performed.
