# 2026-08-18 19:02 — Let generated AI insight span the card

Moved the generated insight and its error message below the AI card header row, so the text can use the entire card width instead of sharing the header row with the generate button.

No checks or commit run, per user instruction.
