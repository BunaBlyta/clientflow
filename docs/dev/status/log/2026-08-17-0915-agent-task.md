### 2026-08-17 09:15 — Codex — mobile Stripe confirmation timing

Changed:
- Updated the native checkout screen to poll the invoice status eight times,
  1.5 seconds apart, after returning from Stripe.
- The app now stays in its confirming state while the webhook is still being
  delivered instead of switching to the incorrect “no payment” state after one
  early check.

Tried and abandoned:
- None.

Left for next session:
- Restart Expo and run a fresh native iOS payment test. If the invoice still
  does not become paid after the polling window, inspect Stripe Event
  Deliveries and the Vercel webhook secret; that would indicate a webhook
  configuration problem rather than a mobile timing problem.

Assumptions made:
- Stripe webhook delivery normally completes within the approximately
  12-second polling window.

Blockers:
- No connected simulator was available for an automated visual click-through.
