# Web lane — make Request own cross-section bubble

## Changed

- Moved the large cross-section bubble into Request a package.
- Let it overflow downward into How it works so the visual originates from Request and continues into the next section.
- Removed the separate How it works bubble and restored its normal overflow behavior.

## Verification

- Not run, per Buna’s instruction to hold checks and commits.
