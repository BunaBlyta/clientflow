### 2026-08-14 09:47 — Agent B — extend gradient across landing page

Changed:
- Moved the softened two-tone gradient to the marketing layout background so it continues behind the full landing page.
- Removed the duplicate hero-only gradient layer, keeping the same shades, opacity, and visual focus near the top.
- Dashboard surfaces remain unaffected.

Tried and abandoned (what didn't work, and why):
- Keeping the gradient in both the layout and hero would stack the color and make the top too bright, so the hero-only layer was removed.

Left for next session:
- The landing page now uses the gradient as a page-wide ambient background and remains uncommitted pending Buna’s instruction.

Assumptions made (flag if wrong):
- “Throughout the page” means a subtle landing-page background wash, not applying the gradient to the staff dashboard or every individual section/card.

Blockers:
- None.
