# Agent B — move gradient to packages boundary

## What changed

- Removed the newly added bottom-origin radial from the hero.
- Added the same soft `#CAF4FF` radial at the bottom of the Packages section, rising from the boundary above Request a package.
- Restored the hero gradient layer to its prior top-only bounds.

## Verification

- Not run by request; no commit created.
