# 2026-08-27 11:18 — Codex — localize the `Invoice issued` notification variant

## Changed

- Updated `mobile/lib/notification-text.ts` to recognize the exact server combination `type: INVOICE_ISSUED` with title `Invoice issued`.
- Reused the existing localized `notifications.invoiceSentTitle` title and `notifications.invoiceDescriptionBody` template, preserving the server-provided invoice description through interpolation.
- Existing `Invoice sent` behavior remains unchanged.

## Verification

- Mobile has no Vitest/Jest configuration or test files, so no focused mobile test was added.
- `npx tsc --noEmit` from `mobile/`: passed.

## Handoff

- No API files were changed. The commit is limited to the mobile formatter plus the mobile lane status/log.
