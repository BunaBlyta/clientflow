# Agent B — match selected dropdown text

- Set the light-mode Request a package dropdown trigger text to the same muted cyan as the placeholders.
- This includes the selected package value, not only the empty state.
- No checks or commit run, per the user’s instruction.
