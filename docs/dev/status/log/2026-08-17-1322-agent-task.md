### 2026-08-17 13:22 — Codex — fix declined Stripe payment retry

Changed:
- Added invoice and project metadata to the Stripe PaymentIntent created by Checkout.
- This lets `payment_intent.payment_failed` identify the invoice and move it from payment processing to failed.
- Added a regression test for the new Checkout request fields.
- Made the mobile checkout show its retry state when Stripe confirmation polling ends without a confirmed result.

Tried and abandoned (what didn't work, and why):
- The default Turbopack production build still fails on the sandbox's port-binding restriction. The Webpack production build passed.

Left for next session:
- Exercise a fresh declined-card checkout in the deployed mobile app and verify the invoice becomes Failed and the retry button opens Checkout again.

Assumptions made (flag if wrong):
- Stripe card declines arrive through `payment_intent.payment_failed`; the new PaymentIntent metadata is therefore the required link back to the invoice.

Blockers:
- No connected mobile simulator or device was available for a fresh click-through.
