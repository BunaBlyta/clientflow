# Agent B — pressed request-field focus

- Removed the outward focus ring/glow from light-mode Request a package fields and dropdown.
- Added a stronger inset shadow on focus so selected controls read as pressed in.
- No checks or commit run, per the user’s instruction.
