# Agent C — integrate landing cyan gradients

## What changed

- Added a shared SVG-backed cyan gradient backdrop using the existing
  react-native-svg dependency. It uses #CAF4FF washes and #5AB2FF
  highlights without adding a package.
- Applied it to shared app screens, authentication screens, and the notes
  composer so the landing-page atmosphere is consistent throughout mobile.
- Replaced the login grid glyph with the existing blue app artwork from
  mobile/assets/icon.png.
- Left the blue artwork itself and all web files unchanged.

## Verification

- Mobile TypeScript passed under Node 22.23.2.
- Root tests passed: 34 files and 145 tests. Root typecheck and lint passed.
- Expo web launched under Node 22.23.2 in offline mode; no reliable local
  browser smoke check was available in the sandbox.
- git diff --check passed.

## Decisions and limits

- The existing cards and information architecture remain in place; the
  landing reference's rounded app preview supports that visual language.
- No install, migration, or destructive command was run.
- Commit and push were deferred because Buna previously requested explicit
  authorization before committing.
