# Agent C — mobile iOS accent and safe-area polish

## What changed

- Changed the light mobile `accentSoft` token from `#EAF5FF` to `#CAF4FF`.
  All shared consumers now inherit the web app's soft accent automatically;
  the primary and pressed accent tokens and the dark token were not changed.
- Added the device bottom safe-area inset to the shared scrollable `Screen`
  component in addition to its existing 32px bottom padding.
- Confirmed that the auth Stack already supplies native iOS headers, chevrons,
  transitions, and swipe-back behavior, and that the requested border cases
  already use `StyleSheet.hairlineWidth`.
- Preserved the existing uncommitted mobile visual pass in project detail,
  notes, invoices, and shared components.

## Verification

- Mobile TypeScript check passed under Node 22.23.2.
- Root tests passed: 34 files and 145 tests. Root typecheck and lint passed.
- Expo web was started under Node 22.23.2 in offline mode. The sandbox could
  not connect to the local server, so no browser smoke check was claimed.
- No physical device or simulator was available for manual edge-swipe testing.

## Decisions and limits

- The `#CAF4FF` mobile token is an intentional deviation from the current
  AGENTS.md wording; the lane state flags this for Buna to decide permanently.
- The request-status screen remains fixture-backed because no public API
  endpoint exists for it.
- No install, migration, or destructive command was run.
- Commit and push were intentionally deferred because Buna explicitly asked
  for authorization before committing.
