# Agent B — fix process cyan override

## What changed

- Fixed the light-mode node color override so How it works icons render in `#A0DEFF` instead of black.
- Fixed the connector override so it renders in `#A0DEFF` instead of gray.

## Verification

- Not run by request; no commit created.
