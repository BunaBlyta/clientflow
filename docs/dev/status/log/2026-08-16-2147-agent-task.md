# Web lane — widen settings dialog

- Increased the Settings dialog from `max-w-2xl` to `max-w-4xl` so its tabs, package rows, and team content have more room.
- Kept settings behavior and section structure unchanged.
- No commit, checks, or push performed; waiting for the user’s instruction.
