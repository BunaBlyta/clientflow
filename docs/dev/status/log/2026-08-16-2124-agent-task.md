# Web lane — continuous hero packages gradients

- Extended the package side gradients into the bottom of the dark hero.
- Applied matching radial gradients directly to the Packages section background.
- Removed the extra pseudo-layer overlap so the transition has one coherent treatment.
- No commit, checks, or push performed; waiting for the user’s instruction.
