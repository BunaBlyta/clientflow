# Web lane — remove spanning packages layer

- Removed the dark `hero-package-sides` layer that crossed the hero, Packages, and Request boundaries.
- Matched the hero and Packages base surface and moved the soft bubbles into a Packages-only pseudo-layer.
- Kept the package cards and Request bubbles unchanged.
- No commit, checks, or push performed; waiting for the user’s instruction.
