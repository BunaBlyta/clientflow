# Agent C — flatten mobile surfaces

## What changed

- Replaced the bordered, shadowed ProjectCard treatment with spacious
  hairline-separated project rows.
- Removed the outer card shell from the project stage tracker.
- Flattened account details into a simple separated information list.
- Flattened checkout summary and request-status result containers into
  typographic sections with top and bottom hairlines.
- Kept note bubbles, status banners, buttons, and segmented preferences because
  they communicate message ownership, state, or interaction rather than acting
  as generic content cards.

## Verification

- Mobile TypeScript, root tests, root typecheck, root lint, and diff checks
  will be rerun for this surface pass.
- No web files were changed.
- Commit and push remain deferred pending explicit authorization.
