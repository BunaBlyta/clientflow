# 2026-08-18 19:22 — Restore three-column turnaround row

Restored the Overview average-turnaround package container to three columns at large widths now that all three packages are always shown. Each package entry still keeps its name and metric in an even internal split.

No checks or commit run, per user instruction.
