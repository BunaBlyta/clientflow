# Agent B — raise package cards

- Added a restrained raised-button treatment to light-mode package cards.
- The cards now have a subtle lower ledge, soft lift shadow, and top highlight so they sit above the pressed housing.
- Dark mode and package layout were left unchanged.
- No checks or commit run, per the user’s instruction.
