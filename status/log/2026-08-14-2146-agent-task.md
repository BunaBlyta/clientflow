# Agent B — lift process title cyan

- Lightened the center of the light-mode Request a package and How it works heading gradients to `#167D99`.
- Left all other marketing title gradients, body text, and dark mode unchanged.
- No checks or commit run, per the user’s instruction.
