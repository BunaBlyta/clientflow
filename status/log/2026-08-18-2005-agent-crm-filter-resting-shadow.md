# 2026-08-18 20:05 — Make CRM filter shadows visible at rest

Strengthened the default shadow on shared CRM search inputs and filter selects so the grey depth is visible even before hover. Hover and focus states retain a slightly stronger shadow.

No checks or commit run, per user instruction.
