### 2026-08-14 09:07 — Agent B — fix logo transparency in dark mode

Changed:
- Replaced `public/logo.png`, which had a checkerboard baked into an RGB image, with an RGBA version containing transparent background pixels.
- Kept the existing navbar and sidebar paths unchanged; both already use `/logo.png`, so the fix applies to marketing and dashboard dark mode.
- Verified the resulting asset has an alpha channel and reran the web test, typecheck, lint, webpack build, and diff checks.

Tried and abandoned (what didn't work, and why):
- The first image edit still displayed the checkerboard because the source image itself contained the checkerboard rather than true transparency. The final asset was processed with the provided background-removal workflow and validated as RGBA.

Left for next session:
- No code changes are needed for logo consumers. The remaining API-lane edits in the shared checkout are not part of this task.

Assumptions made (flag if wrong):
- Replacing the existing `/logo.png` is the intended fix because both web consumers already point at that stable path.

Blockers:
- No connected browser was available for a manual dark-mode click-through; asset alpha validation and production build passed.
