# Agent B — match segmented card rail

- Removed the dashed inset frame from the Custom web app form card.
- Added separated cyan segments along both sides and the bottom to match the segmented top rail.
- No checks or commit run, per the user’s instruction.
