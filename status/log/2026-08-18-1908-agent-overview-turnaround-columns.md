# 2026-08-18 19:08 — Split Overview turnaround rows evenly

Changed each Overview average-turnaround row from content-driven flex spacing to a two-column grid, giving the package name and turnaround metric equal half-widths with the metric right-aligned.

No checks or commit run, per user instruction.
