# Agent B — soften packages boundary gradient

## What changed

- Extended the Packages boundary gradient to 720px and reduced both light- and dark-mode opacities.
- Broadened the radial fades so they spread gently upward.
- Set dark-mode package cards to an opaque `var(--background)` surface so the gradient remains behind them and they match the page background.

## Verification

- Not run by request; no commit created.
