# Agent C mobile task log — 2026-08-14 11:38

Restored visible native minimal chevrons on auth and project-detail stacks.
Project detail headers remain titleless while the page titles stay in the
content, preserving the clean layout and native back gesture.

Made the cyan backdrop opt-in instead of global. It remains on auth screens
and the projects overview, while project detail, notes, invoices, account,
and notifications use the plain app canvas so the treatment feels intentional
instead of repeated on every page.

Commit and push remain deferred until Buna explicitly authorizes them.
