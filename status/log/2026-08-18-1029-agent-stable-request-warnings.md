### 2026-08-18 10:29 — Codex — keep request warnings layout-stable

Changed:
- Aligned the inline warning and alert icon in the label row above each request textbox.
- Kept a stable label-row height so showing a warning does not push the textbox down.

Tried and abandoned (what didn't work, and why):
- A warning block above the input changed the form height when it appeared, so it was replaced with a label-row treatment.

Left for next session:
- Nothing specific.

Assumptions made (flag if wrong):
- Right-aligning the warning in the label row keeps it visibly above the textbox while preserving the form’s position.

Blockers:
- None.
