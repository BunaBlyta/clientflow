# Agent B — move How it works down

- Increased only the How it works top padding from `pt-32` to `pt-40`.
- Kept its existing `pb-48` bottom gap unchanged.
- No checks or commit run, per the user’s instruction.
