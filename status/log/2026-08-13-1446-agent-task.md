# Mobile nested navigation back controls

Date: 2026-08-13 14:46
Lane: Mobile (Agent C)

## Completed

- Added visible in-screen back controls to project notes, project invoices,
  invoice detail, and checkout.
- Changed the project detail “Back to projects” action to use a deterministic
  `/projects` replacement route.
- Used absolute replacement routes for every control so direct Expo web loads
  after a browser refresh do not depend on stack history:
  - Project detail → `/projects`
  - Notes → `/projects/<projectId>`
  - Invoices → `/projects/<projectId>`
  - Invoice detail → `/projects/<projectId>/invoices`
  - Checkout → `/projects/<projectId>/invoices/<invoiceId>`
- Updated checkout's existing success and declined-payment return actions to
  use the same invoice route. Stripe checkout opening, AppState refresh, live
  invoice refresh, and the webhook-confirmed `PAID` gate are unchanged.

## Verification

- `npx tsc --noEmit` from `mobile/`: passed.
- Root `npm run test`: passed — 32 test files, 132 tests.
- Root `npm run typecheck`: passed.
- Root `npm run lint`: passed.
- `npx next build --webpack`: passed.
- `npm run verify`: typecheck, lint, and tests passed; Turbopack build was
  blocked by the sandbox's inability to fetch Inter from Google Fonts.
- `git diff --check`: passed.
- Expo web server did not bind to port 8081 in the sandbox, so no interactive
  Expo web, simulator, physical-device, or native deep-link test was performed.
