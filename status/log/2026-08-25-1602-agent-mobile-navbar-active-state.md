### 2026-08-25 16:02 — mobile lane — refine active navbar state

Changed:
- Restored a quiet rounded background behind the active icon only, without the previous outline treatment.
- Moved the tab labels slightly lower so each label is more clearly separated from its icon.

Tried and abandoned (what didn't work, and why):
- Fully removing active icon backgrounds made the active destination less visually anchored, so the background was restored in a lighter form.

Left for next session:
- No follow-up work from this request.

Assumptions made (flag if wrong):
- The requested active background applies to the icon area, not the entire tab item.

Blockers:
- Browser preview inspection was unavailable because no browser connection was available in this session.
