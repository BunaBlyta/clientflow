### 2026-08-16 23:34 — Agent B — widen Settings modal

Changed:
- Overrode the shared dialog’s desktop `sm:max-w-sm` rule on Settings.
- The modal now keeps its intended wide horizontal layout instead of collapsing to the default narrow dialog width.

Tried and abandoned:
- None.

Left for next session:
- No follow-up needed unless the fixed 1600px panel should be changed to a viewport-relative width.

Assumptions made:
- “Too tight horizontally” referred to the Settings dialog rendering at the shared small-dialog width.

Blockers:
- No browser verification was run.
