# Web lane — make cross-section bubble continuous

## Changed

- Removed the separate large bottom bubble from Request a package.
- Enlarged and repositioned the How it works bubble so one element extends upward into Request a package.
- The cross-section treatment now reads as one continuous bubble instead of two aligned circles.

## Verification

- Not run, per Buna’s instruction to hold checks and commits.
