# Centered status tracker glow

## What changed

- Corrected the project stage tracker indicator geometry so completed,
  current, and future stages share the same centered footprint.
- Replaced the square native shadow around the current stage with a circular
  SVG radial gradient glow.
- Explicitly centered the check icon inside the active/completed circle.

## Verification

- `npx tsc --noEmit` from `mobile/`: passed.
- `npx expo export --platform web` from `mobile/`: passed.
- Root `npm run test`: passed — 34 test files, 141 tests.
- Root `npm run typecheck`: passed.
- Root `npm run lint`: passed.
- `git diff --check`: passed.
- No physical-device or simulator testing was performed.
