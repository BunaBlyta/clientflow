### 2026-08-17 14:08 — Codex — reconcile abandoned Stripe sessions safely

Changed:
- Added opt-in server reconciliation for invoice reads after a client returns from Stripe.
- Expired Checkout Sessions and PaymentIntents in `requires_payment_method` or `canceled` now move a pending invoice to `FAILED`.
- PaymentIntents genuinely in `processing` remain pending, so delayed payment methods are not incorrectly failed.
- Wired mobile return/status checks to request reconciliation and added tests for abandoned, processing, and ordinary reads.

Tried and abandoned (what didn't work, and why):
- A client-side timer alone was not used to mark payments failed because elapsed time cannot prove that Stripe will not still complete the payment.

Left for next session:
- Test the exact empty-Checkout flow after deployment: open Checkout, leave it empty, return to the app, and confirm the invoice becomes Failed with Retry payment available.

Assumptions made (flag if wrong):
- Stripe’s `requires_payment_method` state after an explicit return/check means no payment is currently attached and retry is safe.

Blockers:
- No connected mobile simulator or device was available for the click-through.
