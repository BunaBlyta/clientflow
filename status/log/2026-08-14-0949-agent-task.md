### 2026-08-14 09:49 — Agent B — restore hero-only gradient

Changed:
- Reverted the page-wide gradient experiment.
- Restored the softened two-tone gradient to the hero only, with the marketing layout back to its original background.

Tried and abandoned (what didn't work, and why):
- The page-wide ambient wash was not desired, so it was removed without changing the hero gradient itself.

Left for next session:
- The hero-only gradient remains uncommitted pending Buna’s instruction.

Assumptions made (flag if wrong):
- “Back to how it was” means revert only the latest page-wide background change.

Blockers:
- None.
