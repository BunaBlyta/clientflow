### 2026-08-18 14:21 — Codex — undo table control relocation

Changed:
- Restored the previous separate search/filter row above each searchable CRM table.
- Restored the Projects search/status controls beside the Projects tabs.
- Left all other CRM UI changes untouched.

Tried and abandoned (what didn't work, and why):
- The user preferred the previous detached toolbar placement over controls embedded in the table card.

Left for next session:
- No verification was run, per the user's instruction.

Assumptions made (flag if wrong):
- “Undo” referred only to the immediately preceding table search/filter relocation.

Blockers:
- None.
