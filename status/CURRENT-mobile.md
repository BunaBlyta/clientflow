# CURRENT — mobile lane (Agent C)

Last updated: 2026-08-28 by Claude Code — tightened Account screen's Profile/Settings group spacing so "Confirm log out?" is more likely to be visible without scrolling; rapid multi-tap on a navigating button/row no longer stacks duplicate screens (new `useSingleFire` guard applied across every list-row/CTA navigation); notification icons now vary by type using the app's existing 4 semantic colors (grouped by meaning, not one new hue per type); keyboard now dismisses on scroll app-wide (shared `Screen` component); added phone number validation to Edit profile; fixed "Load older" button showing after a background refresh even when the server confirms no more pages exist; notifications tab: scroll-triggered pagination replaced with a manual "Load older" button, archiving auto-marks read, scroll resets to top on tab return (not on swipe-back from an opened notification)

## Latest verification

- Confirmed `mobile/.env` already points at `https://clientflow-ijdn.vercel.app`.
- A direct request to both the site root and the realtime API origin timed out
  after 20 seconds. The deployment is therefore unavailable from this
  environment; changing the mobile value to the same URL would not fix the
  Ably timeout.
- Once the Vercel deployment responds again, restart Expo with
  `npx expo start -c`; an installed build needs to be rebuilt if its embedded
  API URL changes.

Note: this lane is being worked by both Codex and Claude Code concurrently this week (Buna's call). Each session overwrites this file per the working agreement, so treat it as a merge point, not a single author's log — read `status/log/` for the full narrative if something here looks incomplete.

## Current state

- Working tree clean, `npx tsc --noEmit` passes from `mobile/`.
- Account screen (`app/(app)/account/index.tsx`): the "PROFILE"/"SETTINGS" group labels originally had a 16px gap down to their row group (`preferenceLabel.marginBottom`) plus 12px between groups (`preferenceGroup.marginBottom`). Went 8px/8px, then overshot to 4px/4px reading "bit less" as "less spacing" when it meant "less tightening" — settled at 12px/8px, a lighter touch than either previous pass. Tapping Log out swaps in a taller confirm block (buttons + a "Confirm log out?" caption beneath them), and the extra height on top of the page's existing content could push that caption below the fold on shorter screens. Row heights/tap targets are unchanged — only the whitespace between the two group labels and their boxed rows.
- Fixed a systemic bug: `router.push`/`navigation.navigate` have no built-in de-dup, so mashing a list row or CTA 3 times pushed the destination screen 3 times, and the user had to back out 3 times to get back to where they started. New `lib/use-single-fire.ts` (`useSingleFire`) wraps a callback so repeat calls inside an 800ms window are ignored. Applied to every "open a new screen" action across the client app: project list → project detail, project detail's notes/invoices "view all" + invoice row, project invoices tab → invoice detail, invoice detail → checkout (the payment flow — this one matters most, since 3 checkout screens could mean 3 Stripe sessions), invoices tab → invoice detail, Account's three settings rows, all three notification-row destinations, and the shared `lib/project-tab-navigation.ts` (covers Home's project/invoice cards). Deliberately left the pre-login auth screens (login/forgot-password/verify-code links) unguarded — lower-traffic, single-shot flows; can extend if it comes up there too.
- PM wanted a distinct icon color per notification type. Doing that literally would mean ~6 new colors outside the app's restrained palette (design direction explicitly limits this to one UI accent plus standard semantic status colors), so instead `NotificationRow.tsx`'s new `notificationTone()` groups all 9 types across the 4 colors that already exist by what they mean rather than a 1:1 mapping: accent = informational (request submitted, stage changed, new note), warning = needs the client's attention (invoice issued, extra charge), success = positive outcome (approved, payment succeeded), danger = negative outcome (rejected, payment failed). Combined with the already-distinct Lucide icon per type, this differentiates every type visually without adding new hues. Applies everywhere `NotificationRow` is used (notifications tab, Home preview) since it's the one shared component.
- `components/ui/Screen.tsx` (the shared scrollable-screen wrapper used by nearly every screen except the notes composer, which already had its own handling) now sets `keyboardDismissMode` — `'interactive'` on iOS (keyboard follows the drag), `'on-drag'` on Android — so scrolling a form dismisses the keyboard instead of it staying pinned over the content until the user taps into another field and hits return. Reported specifically against Edit profile but fixed at the shared component so it applies everywhere.
- Edit profile's phone field (`app/(app)/account/edit-profile.tsx`) is client-side validated: optional as before (empty is fine), but a non-empty value must start with `+` (country code required — a bare national number is rejected, per PM feedback) and have 10-15 digits total, or Save is blocked with a localized error. Added a `+1 555 123 4567` placeholder so the required format is visible. Note: iOS's `phone-pad` keyboard doesn't have a dedicated `+` key on the main row (long-press `0`) — worth a real device check that entering `+` isn't awkward enough to need a different keyboard type. The API's `PATCH /api/auth/me` still accepts any trimmed string server-side — that's the API lane's surface, not touched here.
- Fixed a real bug in `store/data-store.ts`'s `refreshNotifications`: `notificationsHasMore` used `state.notificationsHasMore || page.hasMore` on any non-reset refresh (background/realtime), which meant a `true` from earlier could never flip back to `false` even after the server confirmed there was nothing left to load — the notifications tab's "Load older" button could get stuck visible. Now always trusts the just-fetched page's `hasMore` directly.
- Notifications tab (`app/(app)/notifications/index.tsx`): the list used to auto-fetch the next page while scrolling (a scroll-position listener firing `loadMoreNotifications` as you neared the bottom). PM wanted the total list capped with older notifications still reachable, but without that automatic-load feel. Replaced the scroll listener with a "Load older" button shown whenever `notificationsHasMore` is true — same pagination plumbing (`loadMoreNotifications`, 20/page via `NOTIFICATION_PAGE_SIZE`), just user-triggered instead of scroll-triggered. (Home's "Recent activity" preview was untouched — a separate, correctly-scoped idea from earlier today was reverted, see the log entry.) `components/ui/Screen.tsx` now forwards a ref to its inner `ScrollView` (used by the scroll-reset fix below).
- Archiving a notification now also marks it read (`store/data-store.ts`'s `archiveNotification`) — the backend endpoint only supports one or the other per PATCH call, so this fires a second `markNotificationReadRequest` when the notification was unread, only on archive (not unarchive, which leaves read state untouched). This reverses a prior deliberate decision (the removed comment said archiving must stay independent of read state) — a real behavior change per PM's ask, not a bug fix.
- Notifications screen now resets scroll to the top whenever the tab regains focus (tab-bar switch, cold start), except immediately after the user opened a notification from that screen and is returning to it (swipe-back/back-button) — that one case preserves scroll position. Implemented with a ref set right before navigating away from a notification row; it only affects the very next focus event, so it doesn't misfire if the user detours through another tab first.
- Notification archive action: replaced the generic archive box with Lucide's `LibraryBig` books icon, matching PM feedback, while keeping `ArchiveRestore` for the inverse action. The visible icon is 14px and shifted 4px above its top-aligned 24px control; 10px hit slop preserves a 44px effective tap target. Mobile typecheck passes. Root `npm run verify` still stops on the two existing web-lane lint errors in `date-picker.tsx` and `settings-content.tsx`.
- Cross-screen spacing audit: Home already uses `spacing.md` (12px) both between Next payment/Messages and between that pair and the main project card. Invoices already uses the same 12px between Outstanding/Paid to date and between invoice rows. Notifications was the outlier at 32px between date groups; it now uses 12px, removing the oversized break before headings such as Earlier. Mobile typecheck passes. Root `npm run verify` still stops on the two existing web-lane lint errors in `date-picker.tsx` and `settings-content.tsx`.
- Logout confirmation: Cancel and Log out use one shared button base, so their height and corner radius cannot drift apart. Both are 56px tall with `radius.lg`, matching the original full-width logout control they replace; their distinct neutral/error colors and behavior are unchanged. Mobile typecheck passes. Root `npm run verify` still stops on the two existing web-lane lint errors in `date-picker.tsx` and `settings-content.tsx`.
- Semantic feedback colors: existing success badges/messages/icons now use standard green, warnings use orange, and errors use red in both themes. Only the shared `success*`, `warning*`, and `danger*` theme values changed; no badge, message, indicator, or decorative/brand color was added or repurposed. Mobile typecheck passes. Root `npm run verify` still stops on the two existing web-lane lint errors in `date-picker.tsx` and `settings-content.tsx`.
- Checkout handoff: pressing “Continue to secure checkout” immediately enters the loading screen. Missing sessions, API/Stripe errors, polling timeouts, and `FAILED` or other non-paid invoice states all remain on that neutral screen; only a confirmed `PAID` invoice advances to success. The user can still leave through the existing back button. Mobile typecheck passes. Root `npm run verify` reaches lint and stops on the two existing web-lane errors in `date-picker.tsx` and `settings-content.tsx`.
- Invoices tab: the two KPI cards (Outstanding / Paid to date) now use the same inner padding (both 12; "Paid to date" was falling through to the Card default of 20). Also pinned both labels to a single line (`numberOfLines={1}`) — "PAID TO DATE" wrapped to two lines in the narrow card while "OUTSTANDING" stayed on one, pushing the amount below it out of alignment with the other card. Finally, the currency amounts render in Georgia (`fontFamily.serif`), whose default oldstyle figures make digits like 4/5/7/9 dip below the baseline — added `fontVariant: ['lining-nums', 'tabular-nums']` to the KPI value and to `InvoiceRow`'s amount so figures sit on one baseline and align by column.
- Account tab profile card: was showing the client's name twice (the "company" line was `clientFromUser` faking `companyName = user.name`) with a lot of empty padding. Redesigned to a horizontal card — 56px avatar, name + real company name (when present), then a divider and Mail/Phone/"Client since {month year}" detail rows. `companyName`/`phone` are now optional on the mobile `Client` type and flow straight through from the auth payload once the API sends them (it doesn't yet — see next bullet); `memberSince` comes from `user.createdAt`. Rows hide when their value is absent, so pre-existing logged-in sessions just show name + email until next login. See `status/log/2026-08-27-1640-claude-account-profile-card.md`.
- Notes send is optimistic: the message drops into the timeline instantly (no button spinner), shows "Sending…" → "Sent" (tick), then quietly hands off to the confirmed store note. A failed send goes red with tap-to-retry. `NoteBubble` gained `status`/`onRetry` props. See `status/log/2026-08-27-1835-claude-optimistic-note-send.md`.
- Loading states are skeletons now, not blank screens or bare spinners. New `components/ui/Skeleton.tsx` (shared-pulse box primitive + ProjectCard/InvoiceRow/NotificationRow/NoteBubble/KpiCard skeletons) wired into home, projects list, project detail, notes, invoices tab, project invoices, invoice detail, notifications. Home + projects list gained a local `loading` flag (they had none — showed the empty state mid-fetch); project detail no longer flashes "project not found" while loading. See `status/log/2026-08-27-1810-claude-skeleton-loading.md`.
- Account tab is now a route stack like `notifications/` and `invoices/`: `app/(app)/account/index.tsx` (was `account.tsx`) + `app/(app)/account/_layout.tsx`, with `edit-profile.tsx` / `change-password.tsx` / `help-support.tsx` as sibling screens. The separate hidden `settings` tab is deleted. Opening a row from Account now slides a screen in over the Account list and the back arrow / swipe pops straight back — no tab switch, no `source` param. See `status/log/2026-08-27-1745-claude-account-own-stack.md`. Metro needs `-c` to pick up the moved files.
- Swipe-back / hardware back now matches the in-app back button. Screens opened from a tab (they carry a `source` param) had the gesture fall through to a plain stack pop, which from Home landed you on the Projects/Invoices list instead of Home. `useOriginBack` now intercepts the `GO_BACK` via a `beforeRemove` listener and routes it through the same `goBack()`. Needs a device check that native-stack fires `beforeRemove` on the gesture — see `status/log/2026-08-27-1730-claude-swipe-back-matches-button.md`.
- `TextField` (shared, used by all auth screens + the new profile screens): no border in any state now — filled shape only (resting `surfaceMuted`, focused `surface`), matching the notes composer. Validation errors show only as the label under the field; the red error-border was removed. See `status/log/2026-08-27-1710-claude-borderless-textfield.md`.
- Account "Edit profile" and "Change password" screens: built and wired end to end (`app/(app)/settings/edit-profile.tsx`, `change-password.tsx`, reachable from a new "Profile" group on the Account screen). Editable info is name / company / phone (not email — read-only field with a "contact your studio" hint). Buna gave the go-ahead for Claude Code to add the API endpoints too: `GET /api/auth/me` now returns `companyName`/`phone`, new `PATCH /api/auth/me` and `POST /api/auth/change-password`, and `login`/`set-password` include the profile fields so the card fills on login. `store/auth-store.ts` gained `updateClient()` to merge edits back into the session; `TextField` gained an `editable` prop. i18n added for all three languages. See `status/log/2026-08-27-1650-claude-account-edit-profile-password.md` and the note at the top of `status/CURRENT-api.md`. Not yet run on a device.
- Notes composer send button: sized to the text field's 52px resting height (`NOTE_INPUT_MIN_HEIGHT`) instead of a 46px square, sharing the field's corner radius so the two controls match. See `status/log/2026-08-27-1615-claude-send-button-match-field.md`.
- Notes composer text field: inner padding raised to 16px on all sides (was 12px, smaller than the 14px corner radius, so long scrolled messages had their top/bottom lines clipped by the rounded corners); min height 52px. See `status/log/2026-08-27-1348-claude-notes-composer-padding.md`.
- Notes composer: native height auto-sizes to content (compact ↔ capped/scrollable for long drafts), localized character count/error at the API's 10,000-char limit, keeps the full draft on a failed send. After a successful send it resets to compact height and now stays that way — fixed a race where a stray native `onContentSizeChange` for the pre-clear content could fire after the reset and re-expand the box until the user tapped back in; content-size reports are now suppressed after a send until the user actually types again.
- Chat message bubbles support long-press-to-copy (copies the currently displayed/translated text to the clipboard, using react-native's still-functional in-core `Clipboard` — no new package needed), with a brief "Copied" swap in place of the timestamp as confirmation. Disabled on preview bubbles (e.g. Home's recent-activity card).
- Notifications: paginated (20/page, loads more near list end), Active/Archived views (archive/restore via `{ archived: boolean }` on the authenticated endpoint, preserving local read state), realtime/push merges by notification ID without duplicates, timestamps shown inline after the title.
- Human-authored note bodies and new-note notification bodies translate asynchronously through the authenticated `/api/translate` route (deduped/cached in memory; falls back to original content on any provider/route/token error). System note text and templated notification copy stay on the deterministic i18n path.
- Earlier this session (2026-08-27 morning): fixed five PM review notes — see `status/log/2026-08-27-0927-claude-pm-notes-fixes.md` for full detail (composer keyboard-follow animation, warning/error text colors across 5 screens, invoice/checkout radius+spacing consistency, theme-toggle icon flash, Help & Support back-button-to-Home bug) — plus a follow-up pass on the Help & Support page layout and content (icon sizing/row layout per feedback, then fleshed out from two thin paragraphs into four real sections).
- The theme toggle (dark/light crossfade) has a long history of iteration in this project's log — see older log entries for that saga. As of today it's a real screenshot-crossfade (`lib/theme.ts`'s `ThemeProvider`) plus a synced floating-Modal copy of the Account screen's switch and its leading icon so neither desyncs from the frozen screenshot. Considered stable.

## API dependencies / next actions

- The API lane has prepared notification archive persistence, but its Prisma migration is not applied to Neon. Buna must run `npx prisma migrate deploy` once from the repository root after reviewing it; do not run it concurrently with another migration.
- `/api/translate` (server-side DEEPL_API_KEY) must be committed/deployed as an authenticated route — the mobile client already sends `{ text, targetLanguage, sourceLanguage: 'auto' }` and safely falls back to original content when the route is unavailable.
- The paginated notification envelope has no global unread total yet. Loaded/realtime unread state is kept consistent, but an exact badge across unloaded pages needs an API-provided unread count.

## Login reachability finding

- Login is correctly formed as `POST /api/auth/login`; the shown error is only used when `fetch` gets no HTTP response (`ApiError.status === 0`).
- Mobile `.env`'s `EXPO_PUBLIC_API_URL` (`https://clientflow-ijdn.vercel.app`) timed out from this machine while `vercel.com` itself responded. Buna needs to confirm the current production URL, update `.env`, and rebuild/restart Expo so the value is embedded.

## Not verified on-device

- Nothing in this file has been confirmed on a physical device or simulator this session (none was available) — flagging in particular the composer's keyboard-follow animation and the new auto-compact/copy-message fix for a spot-check.
